[pytest]
minversion = 6.0
addopts = -ra
    --cov
    --cov-branch
    --cov-report=html
    --cov-report=term-missing
    --cov-report=xml
    --cov-report=lcov
    --cov-context=test
    --mypy
    --mypy-ignore-missing-imports
log_level: INFO
testpaths =
    tests
